const STATUS = { pending: "pending", accept: "accept", rejected: "rejected", ignore: "ignore" };

const REQ_STATUS = [STATUS.pending, STATUS.accept, STATUS.rejected, STATUS.ignore];

module.exports = { REQ_STATUS, STATUS };
